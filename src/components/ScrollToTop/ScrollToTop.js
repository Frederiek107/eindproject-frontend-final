import React, {useState} from 'react';
import {IoIosArrowDropup} from 'react-icons-fa';

function ScrollToTop() {
    const [showScroll, toggleShowScroll] = useState(false);

    return (
        <>
        </>
    )
}

export default ScrollToTop;